# PvP 초대 코드 방 시스템 TODO

작성일: 2026-08-12

대상: Unity 6.3 LTS / ASP.NET Core 권위 서버 / NCP Ubuntu 24.04

개발 PC 메모리: RAM 16GB + Windows 가상 메모리 사용
데이터 저장 방침: SQL 미사용, JSON 스냅샷 + append-only 저널 사용

## 현재 확인된 상태

- 서버는 현재 `PvpMatchRuntime` 싱글턴 하나만 실행하므로 한 번에 한 경기만 지원한다.
- 기존 HTTP API로 2~4인 턴제 시장 거래 테스트는 가능하다.
- 접속 토큰 인증, 요청 중복 방지, 턴 준비 완료, 턴 제한 시간, JSON 저널 재생은 구현되어 있다.
- Unity에는 직접 서버 주소와 토큰을 입력하는 화면은 있으나 방 생성/초대 코드 참가/대기실 UI는 없다.
- 지도 이동과 점령은 싱글플레이 로컬 서비스에서 처리되며 서버 권위 PvP 명령으로 연결되지 않았다.
- WebSocket 실시간 상태 전송은 없다.
- HIVE 인터페이스와 연결 문서는 있으나 실제 SDK 어댑터는 없고, 현재 코드는 상태 폴링을 전제로 하므로 무료 한도 보호에 적합하지 않다.
- NCP 서버의 Kestrel은 내부 `127.0.0.1:5100` 바인딩 구조를 유지하고 외부 공개는 HTTPS 443 역방향 프록시로 처리해야 한다.

이번 점검 중 만들기 시작했던 미완성 지도 런타임/계약 코드는 방 레지스트리, API, Unity UI와 아직 연결되지 않아 작업 트리에 남기지 않았다. 아래 순서로 한 기능 단위씩 구현하고 검증한다.

## P0: NCP 방 코드 기반 2~4인 다중 방

- [ ] `PvpRoomRegistry`를 만들고 기존 전역 `PvpMatchRuntime` 싱글턴을 방별 런타임으로 변경한다.
- [ ] 6자리 초대 코드는 혼동 문자를 제외한 대문자/숫자로 생성한다. 예: `7KMP4Q`.
- [ ] 같은 코드 충돌 시 재생성하고, 코드 생성 시 암호학적 난수를 사용한다.
- [ ] 방장은 방 생성 시 2~4인 최대 인원을 선택한다.
- [ ] 방 생성자와 참가자에게 각각 256비트 이상의 세션 토큰을 한 번만 반환한다.
- [ ] 서버 저장 파일에는 평문 토큰을 쓰지 않고 SHA-256 해시만 기록한다.
- [ ] 방장이 2명 이상 모인 뒤 시작할 수 있고 시작 후 일반 신규 참가는 차단한다.
- [ ] 참가자 표시 이름은 길이/금칙 문자/중복을 검증한다.
- [ ] 방별 `matchId`, 플레이어 슬롯, 회사 ID, 생성 시각, 마지막 활동 시각, 상태를 보관한다.

예정 API:

```text
POST /api/v1/rooms                         방 생성
POST /api/v1/rooms/{roomCode}/join         초대 코드 참가
GET  /api/v1/rooms/{roomCode}              대기실/방 상태
POST /api/v1/rooms/{roomCode}/start        방장 경기 시작
GET  /api/v1/rooms/{roomCode}/match        재접속 전체 상태
POST /api/v1/rooms/{roomCode}/commands     턴 명령
POST /api/v1/rooms/{roomCode}/ready        턴 준비 완료
```

완료 기준:

- [ ] 한 서버 프로세스에서 서로 영향을 주지 않는 방 3개 이상을 동시에 실행한다.
- [ ] 각 방에 2~4명이 참가하고 5턴 이상 진행한다.
- [ ] 틀린 방 코드, 정원 초과, 시작 후 참가, 다른 방 토큰 사용을 모두 거부한다.

## P0: 재접속, 방 정리, 최대 방 제한

- [ ] 클라이언트는 마지막 방 코드/플레이어 ID/토큰을 `PlayerPrefs`에 저장하지 않는다. OS 보안 저장소가 준비되기 전까지 메모리 유지와 사용자가 명시적으로 선택한 로컬 세션 파일만 사용한다.
- [ ] 같은 토큰으로 재접속하면 같은 플레이어 슬롯과 개인 상태를 돌려준다.
- [ ] WebSocket 종료는 즉시 탈락으로 처리하지 않고 연결 상태와 마지막 확인 시각만 갱신한다.
- [ ] 대기실 유휴 만료 기본값 30분, 종료 경기 보존 기본값 2시간을 환경 변수로 설정한다.
- [ ] 활성 경기의 장기 유휴 만료 기본값 24시간을 둔다.
- [ ] 최대 방 수 기본값 16을 두고 초과 생성은 HTTP 503으로 거부한다.
- [ ] 1GB NCP 서버를 고려해 방별 플레이어/명령/스냅샷 메모리 상한을 둔다.
- [ ] 1분 주기의 정리 서비스가 만료 방을 메모리에서 제거하고 보관 정책에 따라 파일을 이동/삭제한다.
- [ ] 프로세스 재시작 시 방 메타데이터 + 경기 저널 + 지도 스냅샷을 읽어 활성 방을 복구한다.
- [ ] 손상된 파일은 해당 방만 격리하고 서버 전체 시작을 막지 않는다.

환경 변수 초안:

```text
PVP_DATA_DIR=/var/lib/economy-slg
PVP_MAX_ROOMS=16
PVP_LOBBY_IDLE_MINUTES=30
PVP_ACTIVE_IDLE_HOURS=24
PVP_FINISHED_RETENTION_HOURS=2
PVP_FIXED_STEP_HZ=5
```

## P0: Unity 방 생성/참가/대기실 UI

- [ ] 기존 “여러 명이서 하기” 선택 후 `방 만들기`와 `초대 코드로 참가`를 보여준다.
- [ ] 방 만들기: 닉네임, 최대 인원 2~4, 생성 버튼.
- [ ] 참가: 닉네임, 6자리 방 코드, 참가 버튼.
- [ ] 생성 성공 시 초대 코드를 크게 표시하고 복사 버튼을 제공한다.
- [ ] 대기실에 P1~P4, 접속 상태, 방장 표시, 현재/최대 인원을 표시한다.
- [ ] 방장에게만 `경기 시작` 버튼을 보여준다.
- [ ] 서버 메시지와 오류는 한국어로 표시한다.
- [ ] 게임 진입 후 연결 화면은 완전히 닫고 실제 서버 지도를 렌더링한다.
- [ ] 서버 주소 기본값은 개발 시 로컬, 배포 빌드는 HTTPS 도메인으로 분리한다.
- [ ] 기존 직접 토큰 입력은 개발자 메뉴로 이동해 일반 사용자 화면에서 숨긴다.

## P1: 지도 이동/점령의 서버 권위 처리

- [ ] 서버가 방 코드로부터 결정적 지도 시드를 만들고 80x48 평면 맵을 생성한다.
- [ ] 좌우는 순환하고 위아래는 막힌 현재 월드 규칙을 유지한다.
- [ ] 시작 성 2~4개, 빈 성, 광산, 금광, 숲, 목장을 서버에서 생성한다.
- [ ] 클라이언트는 `부대 ID + 목표 좌표 + 명령 종류`만 보낸다.
- [ ] 서버가 소유권, 육지 여부, 좌우 순환 최단 경로, 체력/보급, 충돌을 검증한다.
- [ ] 이동 경로와 현재 위치는 서버가 계산하며 클라이언트가 결과 좌표를 지정하지 못한다.
- [ ] 광산/숲/목장 점령과 성 점령/공성 진행도는 서버 고정 스텝으로 누적한다.
- [ ] 점령 완료 시 서버가 소유권을 바꾸고 클라이언트는 세력 색상과 깃발만 표현한다.
- [ ] 본성 함락, 파산, 12개월 종료, 15턴 이후 경제력 3배를 2개월 유지하는 승리 조건을 서버에서도 동일하게 판정한다.
- [ ] 지도 스냅샷에 지도 크기, 지형, 부대, 경로, 시설 소유권, 공성/점령 진행도를 포함한다.

지도 명령 초안:

```text
MoveUnit
OccupyResourceSite
OccupyCastle
StartSiege
CancelOrder
```

## P1: WebSocket + 고정 스텝

- [ ] `wss://게임도메인/ws/v1/rooms/{roomCode}` 엔드포인트를 추가한다.
- [ ] 연결 직후 개인용 전체 스냅샷을 전송하고 이후에는 revision 기반 변경분을 전송한다.
- [ ] 서버 고정 스텝 기본값은 5Hz로 시작한다. 경제 일자/턴 정산과 지도 이동 스텝은 분리한다.
- [ ] 이동/점령 명령은 HTTP 또는 WebSocket 한 경로로만 권위 처리하고 중복 요청 ID로 멱등성을 보장한다.
- [ ] Ping/Pong, 마지막 수신 revision, 재접속 시 누락 스냅샷 복구를 구현한다.
- [ ] 느린 클라이언트의 송신 큐 상한을 두고 초과 시 연결을 정리한다.
- [ ] 모든 플레이어에게 같은 공개 상태를 보내되 개인 창고/미공개 정보는 각 토큰별로 필터링한다.
- [ ] Unity는 WebSocket 미연결 시 저빈도 HTTP 재접속만 사용하고 상시 폴링하지 않는다.

## P1: HTTPS 443 배포

- [ ] Kestrel은 `127.0.0.1:5100`만 유지한다.
- [ ] Caddy 권장 구성 또는 Nginx 대체 구성을 `Server/Deploy`에 추가한다.
- [ ] 도메인의 A 레코드를 NCP 공인 IP로 연결한다.
- [ ] NCP ACG는 22(관리 IP 제한), 80(인증서 발급/리다이렉트), 443(게임 접속)만 허용한다.
- [ ] Caddy/Nginx가 HTTPS와 WebSocket 업그레이드를 처리하고 5100 포트는 외부에 열지 않는다.
- [ ] systemd 서비스에 전용 비로그인 사용자, 재시작 정책, 환경 파일 권한 600을 적용한다.
- [ ] WinSCP는 배포 묶음 전송에만 사용하고 실행/서비스 관리는 SSH에서 한다.
- [ ] 인증서가 준비되기 전 공개 IP HTTP 연결을 Unity에서 허용하지 않는다.

## P2: HIVE SDK 로그인/매치메이킹 어댑터

- [ ] 방 코드 PvP는 HIVE 없이 완전히 동작해야 한다.
- [ ] HIVE SDK가 없는 Unity 프로젝트도 컴파일되도록 별도 어셈블리/조건부 심볼/리플렉션 경계 중 하나를 사용한다.
- [ ] `IHiveLoginAdapter`와 `IHiveMatchmakingProvider`로 HIVE 의존성을 격리한다.
- [ ] 로그인 결과의 PlayerID를 서버 세션 교환 API로 보내고 HIVE 토큰 자체를 게임 로그에 남기지 않는다.
- [ ] 현재 주기적 `GetStatusAsync` 폴링 루프를 제거하고 HIVE SDK 완료 콜백 기반으로 바꾼다.
- [ ] HIVE 매칭 완료 콜백을 받은 서버가 내부적으로 방을 생성하고 참가자별 짧은 수명의 입장권을 발급한다.
- [ ] HIVE AppID/Server Security Key/Match ID가 없으면 기능을 `비활성`으로 표시하고 API를 호출하지 않는다.
- [ ] 실제 HIVE 연동은 콘솔 값과 공식 SDK가 준비된 뒤 Sandbox에서만 검증한다.

## P2: HIVE 무료 사용량 강제 차단

- [ ] 기본값 `HIVE_ENABLED=false`로 둔다.
- [ ] 월별 Auth/Matchmaking 호출 수를 로컬 JSON으로 기록하고 프로세스 재시작 후에도 유지한다.
- [ ] 무료 한도의 80%에서 경고, 90%에서 신규 자동 매칭을 차단한다.
- [ ] 호출 전 원자적으로 예약 카운트를 증가시키고 실패해도 호출량으로 계산한다.
- [ ] 폴링, 자동 재시도 폭주, 백그라운드 로그인 반복을 금지한다.
- [ ] 운영자가 명시적으로 활성화하지 않으면 HIVE 네트워크 요청이 0회인지 테스트한다.
- [ ] 유료 기능/초과 사용을 자동 승인하거나 자동 결제하지 않는다.

## 테스트 TODO

- [ ] 방 생성/참가/시작/정원/중복 이름/잘못된 코드 단위 테스트.
- [ ] 최대 방 수 도달, 유휴 정리, 종료 방 정리 테스트.
- [ ] 토큰 해시 저장 및 다른 방 토큰 차단 테스트.
- [ ] 4인 × 여러 방 동시 명령 부하 테스트.
- [ ] WebSocket 끊김/재접속/누락 revision 복구 테스트.
- [ ] 서버 강제 종료 후 재시작하여 같은 방/턴/지도 소유권 복구 테스트.
- [ ] 동일 이동 명령 재전송 시 한 번만 적용되는지 테스트.
- [ ] 조작된 좌표, 타인 부대, 바다 이동, 순간 점령 요청 거부 테스트.
- [ ] 1GB NCP 서버에서 메모리 사용량과 GC 정지 시간을 측정한다.
- [ ] Unity EditMode 컴파일/테스트는 `-batchmode -nographics`로 실행한다.
- [ ] 개발 PC에서는 Unity Editor와 서버/테스트를 동시에 띄우지 않는다.

## 16GB 개발 PC 저메모리 작업 규칙

- Unity를 실행하지 않은 상태에서 서버 코드를 먼저 작성한다.
- `dotnet build -m:1`처럼 병렬도를 1로 제한한다.
- 테스트를 방/서버/Unity 순서로 나눠 실행하며 전체 솔루션 동시 검증을 피한다.
- Unity `Library` 폴더를 Git에 포함하지 않으며 중복 프로젝트의 Library를 동시에 유지하지 않는다.
- 브라우저, Unity Hub, GitHub Desktop, Unity Editor를 동시에 모두 켜지 않는다.
- 가상 메모리는 메모리 부족 종료를 늦출 뿐 성능을 높이지 않으므로, 스왑 증가를 해결책으로 간주하지 않는다.
- 각 P0/P1 항목을 작은 커밋으로 나누고 검증 완료 전 다음 단계로 넘어가지 않는다.

## 다음 작업을 재개할 때 첫 순서

1. `PvpRoomRegistry`와 방 저장 모델만 구현한다.
2. 방 API 통합 테스트를 작성하고 저병렬 빌드로 검증한다.
3. 기존 단일 경기 API를 방별 API로 연결한다.
4. Unity 방 만들기/참가/대기실 UI를 연결한다.
5. 여기까지 2~4인 턴제 인터넷 테스트를 통과시킨 뒤 지도 권위/WebSocket으로 넘어간다.

이 순서가 중요한 이유는 방 수명주기와 인증이 먼저 안정되어야 지도 실시간 상태도 정확히 어느 방과 어느 플레이어에게 전달할지 결정할 수 있기 때문이다.
