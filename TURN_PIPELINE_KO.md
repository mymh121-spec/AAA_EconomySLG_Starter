# 턴제 SLG 파이프라인과 규칙

## 한 턴의 의미

플레이어는 계획 단계에서 여러 행동을 예약하고 `턴 종료`를 누른다. 이후 명령, AI, 생산·소비·물류, 시장을 한 번에 정산하고 다음 턴으로 넘어간다. 기본 설정은 `1턴 = 내부 달력 1일`이며, 내부 달력은 운송 기간·급여·연구 기간 계산에만 사용한다.

## 처리 순서

```text
[플레이어 계획]
  행동력 확인 → 구매/판매/건설/미션/연구 명령 예약
        ↓ 턴 종료
[플레이어 명령 실행]
  예약 순서대로 유효성 재검사 후 실행
        ↓
[AI 행동 실행]
  시장 분석 → 구매/판매/생산/투자 결정
        ↓
[세계 정산]
  공장 생산 → 창고 입출고 → 물류 도착 → 시장 거래·가격 계산
  → 거래 대금·재고 이전 → 운영비·이자·부채 → 경제력 갱신
        ↓
[캠페인 판정]
  파산 → 수도 → 경제 패권 → 30턴 순위 확인
        ↓
[결과 보고]
  명령 성공/실패, 거래, 가격, 손익, 사건 표시
        ↓
[다음 턴 계획]
```

## MVP 룰

- 기본 행동력은 턴당 5이다.
- 현재 구현된 시장 주문은 1 행동력을 사용한다.
- 예약 시점과 실행 시점에 명령 유효성을 각각 검사한다.
- 행동력은 명령 예약 즉시 차감되고, 마지막 명령 취소 시 환급된다.
- 턴 정산이 끝나면 행동력이 전부 회복된다.
- 가격은 임의 이벤트가 직접 변경하지 않는다. 생산·소비·재고·운송·거래 결과로만 계산한다.
- 한 턴 가격 변동은 현재 설정상 이전 가격 대비 최대 ±15%이다.
- AI 단계는 교체 가능한 서비스로 분리했다. 현재 `AICompanyTurnService`가 전 턴 시장 부족·과잉을 분석하고 회사 현금·재고·창고 범위에서 구매·판매 주문을 제출한다.

## 30턴 캠페인 승패 규칙

- 캠페인은 최대 30턴이다.
- 플레이어 회사가 파산하면 해당 턴 정산 직후 즉시 패배한다.
- 전쟁으로 플레이어 수도가 멸망하면 해당 턴 정산 직후 즉시 패배한다.
- 모든 경쟁자가 파산하거나 수도를 잃으면 독점 승리한다.
- 경제 패권 판정은 15턴 정산부터 시작한다.
- 플레이어 경제력이 생존 경쟁사 전체 경제력 합계의 3배 이상인 상태를 2턴 연속 유지하면 경제 패권 승리한다. 따라서 가장 빠른 승리는 16턴이다.
- 30턴까지 캠페인이 끝나지 않으면 생존 회사 중 경제력 1위가 승리하며, 공동 1위는 무승부다.

```text
경제력 = 현금 + 재고 가치 + 시설 가치 + 물류 자산 가치
       + 영토 가치 + 기술 가치 + 최근 3턴 평균 영업이익 × 5
       - 부채 - 미지급 비용
```

제거된 회사의 경제력은 순위 및 경쟁사 합계에서 0으로 처리한다. 3배 조건이 한 턴이라도 깨지면 연속 달성 횟수는 0으로 초기화된다.

## 권장 행동력 비용

| 행동 | 비용 | 비고 |
|---|---:|---|
| 시장 구매·판매 주문 | 1 | 현재 구현됨 |
| 기술 연구 지시 | 1 | 연구 포인트 배정 |
| 미션 시작 | 2 | 위험도에 따라 조정 가능 |
| 공장 생산 계획 변경 | 1 | 다음 정산부터 반영 |
| 공장·창고 건설 시작 | 3 | 완료까지 여러 턴 필요 |
| 지역 확장·대형 투자 | 3~5 | 자금과 기술 조건 추가 |

## 코드 진입점

- `SimulationBootstrapper.EndTurn()`: Unity UI 버튼 진입점
- `TurnHudPresenter`: 턴·행동력·예약 명령을 한국어로 표시하는 최소 HUD
- `SimulationEngine.TryQueuePlayerCommand()`: 플레이어 명령 예약
- `SimulationEngine.EndTurn()`: 턴 상태 전환과 전체 정산
- `TurnResolutionOrchestrator.ResolveTurn()`: 생산·시장 세계 정산
- `WorldEconomyTurnService`: 생산·물류·거래·재무·경제력 실제 상태 변경
- `WorldTurnReport`: 생산 성공, 운송 도착, 거래 정산, 회사 재무 결과
- `IAIResolutionService`: AI 회사 행동 교체 지점
- `AICompanyTurnService`: 결정론적 Utility AI 구매·판매 주문 생성
- `TurnReport`: UI와 저장 시스템이 소비하는 턴 결과
- `CampaignVictoryEvaluator`: 파산·수도·3배 패권·30턴 순위를 판정
- `CampaignSession`: 캠페인 참가 회사와 마지막 승패 결과를 보관

## 실시간 Tick 전환 시 원칙

도메인 계산은 그대로 유지한다. `SimulationEngine`의 턴 스케줄러만 실시간 스케줄러로 교체하고, 동일한 명령·물리 흐름·시장 정산 API를 호출한다. 세이브 파일에는 `CurrentTurn`, `CurrentCalendarDay`, 예약 명령, 회사·시장 상태를 저장한다.
