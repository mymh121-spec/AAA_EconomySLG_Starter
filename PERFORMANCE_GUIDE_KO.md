# 경제 시뮬레이션 성능 가이드

## 현재 목표 규모

- 회사 100개
- 공장 1,000개
- 창고 500개
- 지역 50개
- 자원 50종
- 활성 운송 5,000개
- 턴당 시장 주문 10,000개

이 규모까지는 일반 C# 객체와 턴 단위 배치 처리로 구현하고, 실제 프로파일링 결과가 나오기 전에는 ECS로 전환하지 않는다.

## 기본 규칙

1. 회사, 공장, 창고, 주문, 운송마다 MonoBehaviour나 Update를 만들지 않는다.
2. 모든 경제 객체는 턴 정산에서 배열 또는 리스트 단위로 처리한다.
3. 시장과 AI의 반복 구간에서는 LINQ를 사용하지 않는다.
4. 일시적인 리스트는 재사용 가능한 버퍼를 우선 사용한다.
5. UI는 매 프레임 전체 목록을 갱신하지 않고, 턴 결과나 변경 이벤트가 있을 때만 갱신한다.
6. 여러 턴을 연속 계산할 때는 TurnBatchRunner로 프레임당 처리 턴 수를 제한한다.

## 측정 항목

TurnReport.Performance에서 다음 값을 확인한다.

- 턴 정산 처리 시간
- 물리적 공급·수요 Flow 수
- 거래 체결 수
- 가격 변경 수

권장 목표는 개발 PC 기준 턴 정산 5ms 이하이다. 10ms를 넘으면 Unity Profiler와 Deep Profile을 사용해 시장, AI, 생산, 물류 순서로 병목을 확인한다.

## 설정 에셋

Assets/Game/Data/Settings/simulation_settings.asset에서 다음을 조정한다.

- 목표 시장 재고 일수
- 가격 탄력성
- 일일 최대 가격 변동
- 턴당 최대 주문 수
- 기본 행동력과 턴당 내부 달력 일수
- 프레임당 처리 턴 수
- AI 배치 크기
- 공장·창고·차량·직원 운영비
- 부채 이자와 파산 한도

## 향후 최적화 순서

1. LINQ와 임시 컬렉션 제거
2. Dictionary 조회 캐싱
3. AI 회사를 여러 프레임에 분산
4. 지역별 시장 병렬 계산
5. 대규모 수치 데이터를 연속 배열로 전환
6. 실제 병목 구간만 Burst/Jobs 적용

현재 경제 수치는 정확성과 구현 속도를 위해 decimal을 사용한다. 수만 개 이상의 경제 객체에서 수치 연산이 병목으로 확인되면, 가격과 수량을 long 기반 고정소수점으로 이전한다.
