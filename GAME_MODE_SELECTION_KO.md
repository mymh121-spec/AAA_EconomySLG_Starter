# 1인/여러 명 플레이 모드 선택

게임을 플레이하면 별도 시작 씬 구성 없이 한국어 게임 모드 선택 화면이 자동으로 나타난다.

Windows에서 `RUN_SINGLE_PLAYER.cmd`를 더블클릭하면 Unity를 열고 컴파일 완료 후 게임 내부 모드 선택 화면으로 진입한다. 프로젝트가 이미 Unity에 열려 있다면 상단 메뉴 `게임 > 게임 실행`을 선택한다.

현재 Unity Hub가 `C:\Users\andrew\My project`를 열고 있다면, 먼저 `SYNC_FIXES_TO_MY_PROJECT.ps1`를 실행해 수정본을 활성 프로젝트에 복사해야 한다. 이 스크립트는 덮어쓰기 전에 `_codex_backup-날짜` 폴더를 만든다.

PowerShell을 직접 사용하기 어렵다면 `SYNC_FIXES_TO_MY_PROJECT.cmd`를 더블클릭한다.

## 1인 플레이

- 로컬 `SimulationEngine`을 사용한다.
- 플레이어 1개 회사와 기본 AI 경쟁 기업 3개로 시작한다.
- 게임 진입 시 15×15 지도를 생성하고 플레이어는 좌하단 `(0,0)`에서 시작한다.
- 1인 플레이에도 AI 경쟁 기업 3개가 존재하며 나머지 모서리에 서로 다른 색의 본사로 표시된다.
- 일반 광산과 금광 28개를 서로 다른 무작위 칸에 배치한다. 플레이어와 경쟁 기업의 시작 칸에는 광산을 배치하지 않는다.
- 일반 광산은 곡괭이 그림, 금광은 금화 그림으로 구분한다.
- 서버나 접속 토큰이 필요 없다.
- 턴 종료 시 플레이어 명령, AI 행동, 생산, 소비, 시장, 재무, 승패를 PC에서 정산한다.
- 최대 30턴, 턴 15부터 경제력 3배 조건을 검사하는 기존 캠페인 규칙을 사용한다.
- 캠페인이 끝나면 승자·패자·종료 사유를 표시한다. `확인하고 새 게임 시작`을 누르면 경제 상태와 지도를 모두 초기화한다.
- 씬에 자원 에셋 참조가 없어도 `Assets/Game/Data/Resources`의 에셋을 자동으로 읽는다.

## 여러 명 플레이

- `PvpOnlineSessionController`를 사용한다.
- 서버 주소와 32자 이상의 접속 토큰을 입력한다.
- 토큰은 연결에만 사용하고 입력창과 Unity 에셋에 저장하지 않는다.
- 개발 환경에서는 SSH 터널 주소 `http://127.0.0.1:5200`을 사용한다.
- 공인 서버는 HTTPS만 허용한다.
- 모든 참가자가 준비하면 서버가 권위 있게 다음 턴을 정산한다.

## 화면 흐름

```text
게임 실행
  └─ 모드 선택 화면(맵과 게임 에셋은 아직 생성하지 않음)
       ├─ 1인이서 하기
       │    └─ 15×15 맵 + AI 적 3개 생성 + 로컬 경제 에셋 로드
       │         └─ 캠페인 종료 → 최종 결과 확인 → 새 게임 초기화
       └─ 여러 명이서 하기
            └─ 맵 생성 + PvP 세션 생성
                 └─ 서버 주소/토큰 입력 + 상태 동기화
```

`모드 선택으로` 버튼을 누르면 맵과 로컬 실행 대상을 숨기고 PvP 연결을 종료한 뒤 두 버튼만 있는 시작 화면으로 돌아간다. 서로 다른 두 실행 경로가 동시에 경제를 정산하지 않도록 `GameModeSelection`이 한 번에 한 모드만 허용한다.

모드를 선택한 뒤에는 전체 화면 메뉴 배경이 투명해지고 15×15 맵이 펼쳐진다. 턴 상태와 조작 버튼은 맵을 가리지 않도록 좌측 상단의 작은 반투명 HUD로 표시한다. 맵의 칸을 클릭하면 좌표, 지역 종류, 가능한 행동 안내가 HUD에 표시된다.

## 주요 코드

- `Assets/Game/Scripts/Application/Session/GameModeSelection.cs`: 모드 상태와 상호 배제 규칙
- `Assets/Game/Scripts/Presentation/GameModeSelectionController.cs`: 자동 시작 메뉴와 UI 전환
- `Assets/Game/Scripts/Presentation/SimulationBootstrapper.cs`: 1인 플레이 로컬 경제
- `Assets/Game/Scripts/Presentation/PvpOnlineSessionController.cs`: 여러 명 플레이 서버 세션

현재 여러 명 플레이는 설정된 단일 2~4인 경기 접속이다. 방 목록, 방 만들기, 초대 코드는 로비/`MatchRegistry` 단계에서 추가한다.
