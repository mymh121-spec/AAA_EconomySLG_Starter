# 빈 성 점령과 공성 전환 규칙

## 현재 목표

현재 구현은 **1인 플레이를 기준으로 완성**한다. 플레이어와 AI는 같은
`RealtimeMapGameplayService` 규칙을 사용한다. Unity 화면은 명령을 전달하고
표시만 담당하며, 소유권 판정은 `MonoBehaviour` 밖에서 처리한다.

## 점령 흐름

```text
부대 선택
  → 빈 성 우클릭
  → 빈 성으로 이동·점령
  → 성 도착
  → 점령 진행도 누적
  → 소유 세력 변경
  → 세력 색상 바닥과 깃발 표시
  → 보급/산업/군사/항구 역할 선택
```

- 부대가 성을 떠나거나 여러 세력이 동시에 경합하면 중립 성 점령 진행도는
  초기화된다.
- 점령 완료 시 성에 남은 새 소유 세력 부대 ID를 주둔군으로 기록한다.
- 주둔 부대가 이동하면 다음 고정 스텝에 주둔군 목록에서도 제거된다.
- AI도 거리상 가장 가까운 광산과 빈 성을 비교해 같은 이동·점령 규칙을 쓴다.

## 공성 전환

- 다른 세력이 소유한 성에 공격 부대가 도착하면 성 상태는 `Siege`가 된다.
- 수비대가 한 부대라도 남아 있으면 소유권은 자동으로 바뀌지 않고 공성
  진행도는 0에서 대기한다.
- 수비대가 없어진 성은 무방비 성으로 간주하고 공성 점령 시간이 누적된다.
- 무방비 공성이 완료되면 소유권과 깃발이 공격 세력으로 변경되고 기존 역할은
  초기화된다.
- 다음 전투 구현에서는 수비대가 있는 공성 상태를 야전/공성 전투 판정에
  연결한다.

## 성 역할

| 역할 | 현재 구현 | 다음 경제·군사 효과 |
|---|---|---|
| 보급 거점 | 주둔 3, 징집 2, 2일 회복 | 보급 반경, 이동 회복, 수송 중계 |
| 산업 도시 | 주둔 2, 징집 1, 3일 회복 | 공장 슬롯, 생산 보너스, 창고 확장 |
| 군사 요새 | 주둔 5, 징집 4, 매일 회복 | 수비 보너스, 공성 지연 |
| 항구 | 해안 전용, 주둔 2, 징집 2, 2일 회복 | 승선, 해상 수송, 봉쇄, 상륙 |

맵 생성기는 항구 역할이 막히지 않도록 가능한 경우 빈 성 8개 중 최소 1개를
해안에 배치한다.

## 싱글 우선, 멀티 전환 경계

싱글에서는 로컬 `RealtimeMapGameplayService`가 권위 상태다. 멀티플레이로
전환할 때는 이 서비스를 서버에서 실행하고 클라이언트가 다음 명령만 보낸다.

```text
성 점령 명령 = 경기 ID + 명령 ID + 유닛 ID + 목표 좌표 + 예상 리비전
역할 변경 명령 = 경기 ID + 명령 ID + 성 좌표 + 역할 + 예상 리비전
```

- 세력 ID는 클라이언트 입력을 믿지 않고 인증 세션에서 서버가 결정한다.
- 서버가 유닛 소유권, 좌표, 이동 가능 여부, 성 소유권, 해안 여부를 재검사한다.
- 서버 스냅샷에는 성 좌표, 소유 세력, 점령 세력, 진행도, 역할, 주둔군 ID를
  포함한다.
- 세력 색상·깃발·버튼 문구는 스냅샷을 읽어 그리는 클라이언트 표시 데이터다.
- 현재 단계에서는 싱글 UI만 완성하며, 위 명령 DTO와 서버 저장은 PvP 지도
  상태 동기화 단계에서 추가한다.
