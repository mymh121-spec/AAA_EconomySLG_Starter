# 대형 평면 월드 맵 규칙

## 기본 규격

- 기본 크기: `80 × 48` 타일
- 에디터 조정 범위: 가로 `40~160`, 세로 `24~100`
- 표현 방식: 높낮이 없는 평면 격자
- 지형: 바다, 평원, 숲, 사막, 구릉, 툰드라
- 기본 광산 수: 160개. 맵이 커져도 전체 타일의 최소 3%는 광산 후보로 유지한다.
- 주인 없는 빈 성: 기본 8개. 회색 무소유 표시와 깃발 없는 성으로 표현한다.
- 플레이어와 AI 본사에는 광산이 생성되지 않는다.
- 본사, 빈 성, 광산은 바다에 생성되지 않는다.
- 빈 성은 시작 성·광산과 겹치지 않으며, 시작 성에서 최소 7칸, 다른 빈 성에서 최소 6칸 간격을 우선한다.

## 지구본식 가로 연결

동쪽과 서쪽은 서로 연결된다. 세로는 연결되지 않으며 북쪽과 남쪽 끝이 실제 경계다.

```text
서쪽 끝 ← [0][1][2] ... [77][78][79] → 동쪽 끝
             ↑                         ↓
             └────── 서로 인접 ───────┘
```

- `(79, 10)`에서 동쪽으로 1칸 이동하면 `(0, 10)`이다.
- `(0, 10)`에서 서쪽으로 1칸 이동하면 `(79, 10)`이다.
- `(0, 10)`과 `(79, 10)`의 거리는 79칸이 아니라 1칸이다.
- `(10, 0)`에서 남쪽 또는 `(10, 47)`에서 북쪽으로는 이동할 수 없다.

이동, 사거리, 물류 거리, AI 경로 비용은 직접 `X`를 빼지 말고 반드시 `GridMapLayout.Move`, `Normalize`, `ManhattanDistance`를 사용해야 한다. 그래야 싱글과 서버의 판정이 같아진다.

## 카메라 조작

- 이동: `WASD`/방향키 또는 마우스 가운데 버튼을 누른 채 드래그
- 확대/축소: 마우스 휠
- 내 세력 중앙으로 복귀: `L`
- 일시정지/재개: `Space`
- 타일 선택: 좌클릭
- 부대 확인·미션·이동·채광/점령 행동 메뉴: 우클릭
- 배속 변경: HUD의 1~5배속 버튼, 일시정지/재개는 HUD 버튼 또는 `Space`
- 동쪽이나 서쪽으로 계속 이동하면 반대편으로 자연스럽게 돌아온다.
- 북쪽과 남쪽은 화면 밖으로 넘어가지 않는다.
- 맵을 좌·중앙·우 세 장으로 이어서 표시하므로 가로 경계 가까이에서도 빈 공간이 나타나지 않는다.

## 성능 규칙

이전 방식처럼 타일마다 큐브와 콜라이더를 만들지 않는다. 전체 지형은 런타임 텍스처 한 장과 평면 메시 하나를 좌우에 복제해 표시한다. 클릭 판정도 표면 콜라이더 3개에서 월드 좌표를 격자 좌표로 환산한다.

따라서 기본 3,840개 타일이 생겨도 타일용 GameObject와 Collider는 각각 3개뿐이다. 광산, 본사, 빈 성 표식만 별도 오브젝트로 유지한다.

## PvP 동기화 규칙

- 서버는 월드 시드, 가로·세로 크기, 정규화된 좌표만 권위 데이터로 보관한다.
- 클라이언트가 `X=-1` 또는 `X=80` 같은 좌표를 보내도 서버에서 각각 `79`, `0`으로 정규화한다.
- 세로 범위를 벗어난 명령은 거부한다.
- 경로와 사거리 계산은 서버와 클라이언트가 동일한 가로 래핑 공식을 사용한다.
- 화면용 좌우 복제 오브젝트는 렌더링 전용이며 저장하거나 네트워크로 전송하지 않는다.
