# 자율 세계 핵심 시뮬레이션 프로토타입

## 구현 목표

이 프로토타입은 콘텐츠 양보다 아래 순환이 실제 코드에서 한 턴마다 연결되는 것을 우선한다.

```text
시드 기반 초기 세계
  ↓
자원 채취 + 시설 생산 + 군대 소비
  ↓
지역 시장 수요·공급·재고·가격 변화
  ↓
시장 부족과 세계 상태에서 사건 발생
  ↓
플레이어 개입 미션 또는 NPC 자동 처리
  ↓
자원지·시설·지역 안정도 변화
  └──────────────────── 다음 턴 생산으로 환류
```

가격은 사건 코드가 직접 올리거나 내리지 않는다. 사건은 광산 가동률, 농장 생산량, 시설 생산 차질, 군수 수요 같은 물리량을 바꾸며 `MarketManager`가 그 결과를 보고 가격을 계산한다.

## 한 턴 처리 순서

1. 플레이어가 행동력 안에서 시장 주문 또는 세계 사건 개입 명령을 예약한다.
2. 예약 명령을 실행한다. 사건 개입은 성공 시 문제를 즉시 해결하고 돈·명성을 지급한다.
3. 기존 회사 AI가 이전 시장 상태를 분석해 구매·판매 주문을 제출한다.
4. 자율 세계가 만료 사건과 무시한 미션을 처리한다. 미션 기한이 지나면 지역 NPC가 능력과 주도성으로 자동 해결을 시도한다.
5. 시드와 턴 번호로 랜덤 사건을 판정한다.
6. 이전 턴의 실제 미충족 수요와 지역 도적 위험에서 인과 사건을 만든다.
7. 활성 사건이 광산과 생산시설의 가동 효율을 바꾼다.
8. NPC 생산시설과 군대가 지역 시장에 공급·수요를 제출한다.
9. 자원지가 매장량을 사용해 채취하고 회사 공장이 창고 자원을 사용해 생산한다.
10. 운송 도착을 반영한 뒤 모든 물리 흐름을 지역별 시장에 합산한다.
11. 시장 거래, 재고, 미충족 수요, 가격을 정산한다.
12. 거래 대금, 회사 운영비, 세력 시설비, 군대 유지비, 경제력을 정산한다.
13. 파산·수도 멸망·15턴 이후 3배 경제력·30턴 순위를 판정한다.

## 랜덤 세계

`ProceduralWorldGenerator`는 `System.Random(seed)`만 사용한다. 같은 설정과 seed를 사용하면 지형, 세력, 관계, NPC, 생산시설, 초기 자원지와 지역 경제 계수가 재현된다. 현재 기본값은 지역 6개, 세력 3개, 정착지 5개, NPC 12명, 초기 자원지 8개다.

생성되는 초기 조건:

- 지형: 평원, 숲, 구릉, 산맥, 건조지, 해안
- 지역: 인구, 숲 밀도, 광물 잠재력, 비옥도, 안정도, 도적 위험
- 정착지: 마을, 도시, 수도
- 세력: 국고, 산업 성향, 군사 성향, 확장 성향
- 외교: 관계 점수에서 동맹·우호·중립·경쟁·적대·전쟁 상태 파생
- NPC: 역할, 소속, 지역, 능력, 주도성, 생존 상태
- 시설: 농장, 벌목장, 작업장과 실제 유지비·가동률
- 지역 경제: 자원별 초기 공급·수요·재고 계수
- 자원지: 자원, 매장량, 초기/최소 생산량, 노동력, 소유 세력, 채취 방식

플레이어는 세계 안의 `PlayerCharacterState` 한 인물로 존재한다. 현재 지역, 전투·거래·지휘 능력, 개인 자금, 명성을 가지며 기존 회사는 이 인물이 소속된 경제 조직으로 연결된다. 미션 성공률은 미션 종류에 맞는 인물 능력과 명성으로 계산되고, 보상은 개인 상태와 소속 회사 경제에 함께 반영된다.

## 자원지

`ResourceExtractionSite`는 다음 상태를 가진다.

- 총매장량과 잔여 매장량
- 시간 경과 생산량 감소율과 최소 생산량
- 잔여 매장량 75% 아래에서 시작되는 추가 효율 저하
- 상시 채취 효율, 턴 한정 사건 보정, 노동 충족률
- 노천, 심층, 계획 벌목, 기계화, 고급 탐사의 채취 방식
- 소유 세력과 가동 여부

`Extract()`만 실제 매장량을 줄인다. `GetOutput()`은 UI 예측과 테스트를 위한 비파괴 계산이다. `DevelopDeepLayer`, `DiscoverAdditionalReserve`, `ImproveEfficiency`, `ChangeMethod`를 기술·투자 명령에서 호출할 수 있다.

기존 규칙대로 매 5턴마다 신규 채굴지가 하나 발견된다. 신규 채굴지도 유한 매장량을 가지며 다음 턴부터 자율 사건 대상에 포함된다.

## 병종·장비·경험치

고정 베이스 병종은 검병, 창병, 둔기병, 궁병, 돌팔매병, 기마병 여섯 종류다. 병종 정의와 장비를 분리했기 때문에 중장비 검병과 중장비 창병은 서로 다른 병종 정체성을 유지한다.

- 검병: 범용 근접과 진형 압박
- 창병: 높은 진형 의존도와 대기병
- 둔기병: 중장갑의 낮은 둔기 방호를 이용
- 궁병: 두 번의 접근 사격, 높은 관통 피해, 큰 근접 페널티
- 돌팔매병: 둔기형 원거리 피해로 중장갑 대응
- 기마병: 높은 이동력과 돌격, 접근 사격 횟수 감소

방어구는 무갑·경장갑·중장갑 프로필이며 베기·관통·둔기 피해 감소율이 각각 다르다. 전투는 `원거리 접근 → 돌격 → 근접 → 종료` 단계로 처리된다. 궁병과 돌팔매병은 접촉 전 실제 피해를 주지만 근접 단계에서는 병종별 페널티를 받는다.

경험치는 부대 전체 누적 경험치와 병사 수로 평균을 계산한다. 신병을 충원하면 총경험치는 그대로이고 인원만 늘어 평균 숙련도가 내려간다. 신병·숙련·베테랑·정예 단계는 명중, 사기 저항, 진형 유지, 명령 반응에 서로 다른 보정을 준다.

## 군수와 강대국 비용

군수품이 없어도 부대 객체와 병력은 존재할 수 있다. 대신 식량·강철·의약품의 지역 재고에서 보급률을 계산하고 준비도, 모집 속도, 손실 보충 속도에 단계적 보정을 준다.

군대는 매 턴 병사 급료, 병종 유지비, 장비 유지비를 지출한다. 동원 시 동원 비용과 본거지 거리별 수송 비용이 추가된다. 세력 국고가 부족하면 보급률과 준비도가 떨어진다. 인위적인 국가 규모 페널티는 없다.

## 사건과 미션

랜덤 사건은 시드와 턴으로 재현 가능하다. 현재 흉작, 풍년, 광산 붕괴, 신규 광맥, 도적 증가, 중요 NPC 사망을 지원한다.

인과 사건은 이전 시장 정산의 미충족 수요와 지역 도적 위험에서 발생한다. 군수 자원 부족은 군수품 부족 사건, 일반 자원 부족은 생산 차질 사건으로 바뀐다. 한 턴에 생성할 인과 사건 수와 같은 지역·자원의 반복 발생 대기 턴을 설정에서 제한해 사건 폭증을 막는다.

부정 사건은 도적 토벌, 수송 호위, 광산 복구, 시설 보호, 긴급 납품, 지역 안정화 미션으로 바뀐다. 플레이어가 `SimulationBootstrapper.TryQueueWorldIntervention()`을 호출하면 2 행동력을 쓰는 턴 명령으로 예약된다. 무시하면 기본 3턴 뒤 NPC가 자동 처리하며, 실패 시 사건이 악화되고 다시 개입 기회가 생긴다.

## 밸런스 데이터

`Assets/Game/Resources/SimulationSettings.asset`에서 다음 값을 바꿀 수 있다. 1인 모드가 런타임에 생성되어도 이 에셋을 자동으로 읽는다.

- 세계 seed와 지역·세력·NPC·자원지 수
- 지역 인구와 자원 매장량/생산량/감소율
- 랜덤 사건 확률, 인과 사건 부족 기준, 턴당 사건 상한, 반복 대기 시간
- NPC 자동 처리 지연과 성공률
- 플레이어 개입 효율, 돈·명성 보상
- 사건 생산 페널티, 풍년 보너스, 신규 광맥 매장량
- 병사 급료, 동원·거리 비용, 식량·장비·의약품 수요

`Assets/Game/Resources/MilitaryBalance.asset`은 여섯 병종의 공격, 방어, 이동, 사기, 접근 사격, 명중, 근접 페널티, 돌격, 대기병, 진형, 유지비, 피해 채널을 Inspector에서 덮어쓸 수 있다. 에셋에 누락된 병종은 프로토타입 기본값을 사용한다.

## 주요 코드

- `Domain/World/ProceduralWorld.cs`: 초기 세계 생성과 상태
- `Domain/Resources/ResourceExtractionSite.cs`: 채취·매장량·개발
- `Domain/Military/MilitaryModels.cs`: 병종·장비·경험·군수·전투
- `Domain/World/WorldSimulationModels.cs`: 사건·미션·자율 세계 상태
- `Application/World/AutonomousWorldSimulationService.cs`: NPC 처리와 세계 순환
- `Application/World/WorldEconomyTurnService.cs`: 회사 경제와 자율 세계 물리 흐름 통합
- `Presentation/SimulationBootstrapper.cs`: Unity 새 게임 구성
- `Presentation/TurnReportKoreanFormatter.cs`: 한글 턴 결과

## 검증

`Validation/CoreSimulationSmoke`는 동일 seed 재현, 자원 고갈과 심층 개발, 방어구 상호작용, 신병 숙련도 희석, 접근 사격, 군수 수요, 사건→미션→NPC 처리→시장 가격 환류를 전체 캠페인 길이인 30턴 동안 실행한다.

`Validation/UnityCoreCompile`은 Unity 6.3 LTS의 `UnityEngine.CoreModule`을 참조하여 Domain, Application, Data, `SimulationBootstrapper`를 에디터 실행 없이 컴파일한다.

```powershell
dotnet run --project Validation\CoreSimulationSmoke\CoreSimulationSmoke.csproj
dotnet build Validation\UnityCoreCompile\UnityCoreCompile.csproj
dotnet build Server\Game.Server\Game.Server.csproj
```

## 다음 확장 경계

현재 전투 해석기는 독립 실행 가능하지만 세력 AI가 외교 관계를 보고 전쟁을 선포하고 군대를 이동시키는 자동 전략 계층은 아직 연결하지 않았다. 다음 구현은 `FactionStrategicAI`가 적대 관계·목표 지역·보급 비용을 평가해 `DeployArmyCommand`와 `BattleResolver`를 호출하고, 전투 피해가 수도·광산·운송로에 반영되게 하는 것이다.

플레이어 미션은 명령 API와 결과 보고까지 연결되어 있으나 전용 선택 패널은 아직 없다. UI는 `CurrentAutonomousWorld.Opportunities`에서 `Offered` 상태를 목록으로 표시하고 선택 시 `TryQueueWorldIntervention`을 호출하면 된다.
