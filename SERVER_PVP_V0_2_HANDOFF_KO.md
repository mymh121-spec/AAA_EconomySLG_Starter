# PvP 서버 0.2.0 인수인계

## 완료된 것

- 서버 권위형 2~4인 단일 경기
- 실제 경제 시뮬레이션과 시장 가격 변동
- 플레이어별 행동력, 순번, 턴·리비전 검증
- 명령/준비 요청 멱등 처리
- JSONL 저널과 원자적 JSON 스냅샷
- 재시작 결정론적 복구와 상태 해시 검증
- 턴 제한시간 및 자동 준비
- UnityWebRequest 기반 온라인 클라이언트
- 공인 평문 HTTP 차단, 로컬 SSH 터널 허용
- Linux x64 자체 포함 배포 패키지와 롤백 배포 스크립트

## 검증 결과

- 서버 Release 빌드: 성공
- Unity 6.3 엔진 어셈블리를 참조한 온라인 클라이언트 컴파일: 경고 0, 오류 0
- 로컬 HTTP 2인 턴: 구매/판매 → 두 플레이어 준비 → 실제 시장 가격·현금 변경 확인
- 같은 requestId 재전송: 중복 실행 없이 replay 응답 확인
- 서버 재시작: 동일 턴, 리비전, 시장가격, 현금, 상태 해시 복원 확인
- Unity Test Runner: 현재 PC의 Headless 라이선스 IPC 문제로 이번 변경분 전체 실행은 보류

## 아직 실제 서버에 반영하지 않은 이유

현재 Codex 작업 권한은 이 워크스페이스만 쓰기 가능하므로 `C:\Users\andrew\My project`에는 병합할 수 없었다. 워크스페이스의 전용 SSH 키는 확인했지만 Codex 샌드박스가 외부 22번 포트를 차단해 라이브 VPS의 기존 0.1.0 프로세스도 변경하지 않았다. `Server/Deploy/deploy_from_windows.ps1`을 일반 로컬 PowerShell에서 실행하면 남은 배포 절차를 자동 수행한다.

## 다음 실행 순서

1. 활성 Unity 프로젝트에 `Assets/Game/Scripts` 변경분을 병합한다.
2. Unity Hub 로그인을 정상화하고 EditMode 테스트를 실행한다.
3. 배포 압축 파일을 VPS의 `incoming` 디렉터리로 전송한다.
4. VPS에 `Server/Deploy/deploy_user.sh`를 놓고 실행한다.
5. `Server/Deploy/verify_remote.sh`로 두 토큰의 완전한 한 턴을 검증한다.
6. SSH 터널을 열고 Unity에서 두 클라이언트를 실행해 재접속까지 확인한다.

## 출시 전에 반드시 추가할 것

- 로비, 방 생성/참가, 여러 경기 동시 실행을 위한 MatchRegistry
- 계정 인증과 짧은 수명의 세션 토큰 발급
- HTTPS 도메인과 리버스 프록시
- 서버 프로세스 systemd 등록 및 로그 로테이션
- 경기별 보관/삭제 정책과 백업 도구
- 명령 종류 확장: 공장 건설, 생산 계획, 기술, 미션, 전쟁
- 부하/악성 입력/패킷 유실/재접속 테스트

현재 버전은 친구 2~4명이 한 방에서 플레이하는 개발용 PvP 기반으로는 사용할 수 있다. 불특정 다수에게 공개하는 운영 서비스 단계는 아니다.
